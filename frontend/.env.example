# Frontend env template — copy karke .env.local (dev) banao.
# Production (Netlify) mein .env.production use hota hai, ya Netlify dashboard -> Environment variables.

# Backend base URL (trailing slash optional)
VITE_API_URL=http://localhost:5000
